import * as d3 from "d3";
import us_data from '../data/us.json'
import visited from '../data/visited.json'

export const map = () => {
  //draw the US map divided by states here and color in the states you've visited
  
}